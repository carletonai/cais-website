/// <reference types="vite/client" />

/** Google Calendar id for the events page embed, injected by vite's `define`
 *  from VITE_GOOGLE_CALENDAR_ID. Empty string hides the embed. */
declare const __GOOGLE_CALENDAR_ID__: string;
